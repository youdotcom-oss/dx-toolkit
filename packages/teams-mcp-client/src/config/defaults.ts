// Default configuration values for teams-mcp-client

/**
 * Default MCP server URL (production remote server)
 */
export const DEFAULT_MCP_URL = 'https://api.you.com/mcp';

/**
 * Default request timeout in milliseconds (30 seconds)
 */
export const DEFAULT_TIMEOUT = 30000;

/**
 * Default debug mode (disabled)
 */
export const DEFAULT_DEBUG = false;

/**
 * Default headers (empty object)
 */
export const DEFAULT_HEADERS: Record<string, string> = {};
