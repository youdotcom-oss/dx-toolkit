name: "Package: publish @youdotcom-oss/n8n-nodes-youdotcom"

on:
  workflow_dispatch:
    inputs:
      version:
        description: "New version tag (e.g., 0.1.0)"
        required: true
      next:
        description: "Next prerelease number (optional)"
        required: false

permissions:
  contents: write
  id-token: write
  issues: write

jobs:
  publish:
    uses: ./.github/workflows/_publish-package.yml
    secrets:
      PUBLISH_TOKEN: ${{ secrets.PUBLISH_TOKEN }}
    with:
      npm_package_name: "@youdotcom-oss/n8n-nodes-youdotcom"
      version: ${{ github.event.inputs.version }}
      next: ${{ github.event.inputs.next }}
